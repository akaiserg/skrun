## 2026-07-09T02:47:08Z — Task 1: Add regression test capturing the `forceRemove` Windows path-safety gap
- **Status**: DONE
- **Changed**: `test/fsutil.test.js`
- **Verified**: `node --test test/fsutil.test.js` — 6 pass, 1 fail. The one failure is the new Windows-gap assertion (`3 !== 2`, expected 2 got 3), which is the intended, expected-to-fail outcome documenting Gap G-1. All other assertions (including the new POSIX regression case) pass.
- **Decisions/notes**:
  - `os.tmpdir()` on macOS resolves to a deep path (`/var/folders/.../T`), not `/tmp`, so the POSIX 2-segment regression test uses the literal `/tmp` root directly (`join('/tmp', 'skm-test-guard-regress')`) to get a real 2-segment resolved path, per the task's guidance.
  - Could not reproduce the Windows drive-letter case end-to-end via `forceRemove()` on a POSIX test runner: `path.resolve()` on POSIX treats a non-`/`-leading string like `C:\Users\name` as relative and prefixes it with `cwd`, and the guard's split regex (`/[\\/]/`) also splits on the literal backslashes in `process.cwd()`'s... (n/a, cwd has no backslashes) but more importantly the resulting segment count is polluted by cwd depth, not the drive-letter bug itself. Per the task's explicit fallback instruction, asserted directly on the depth-count formula (`split(/[\\/]/).filter(Boolean)`) applied to the literal string `'C:\\Users\\name'`, mirroring exactly what `resolved.split(...)` would compute on a real Windows host. This is a documented, task-sanctioned deviation from calling the public API directly for this one case.
  - Did not modify `src/fsutil.js` (no export added), per Task 1 constraints.
- **Next**: Task 2 — Fix the `forceRemove` Windows drive-letter path-safety guard

## 2026-07-08T00:00:00Z — Task 2: Fix the forceRemove Windows drive-letter path-safety guard
- **Status**: BLOCKED: Task-1's Windows-gap test assertion (`test/fsutil.test.js:90-117`) cannot pass regardless of any `src/fsutil.js` change — it computes `'C:\\Users\\name'.split(/[\\/]/).filter(Boolean)` as a hardcoded literal expression and asserts `.length === 2`, without importing or calling any function from `src/fsutil.js`. That literal always yields `['C:','Users','name']` (length 3), so the assertion is structurally decoupled from the implementation it's meant to regression-test.
- **Changed**: `src/fsutil.js` — implemented the fix exactly as specified in tasks.md (detect `/^[a-zA-Z]:[\\/]/`, resolve Windows-style paths with `path.win32.resolve`, exclude the drive-letter segment from the depth count, POSIX path byte-for-byte unchanged). This fix is correct per SPEC.md AC-7/Gap G-1 and is left in place, but could not be verified end-to-end because the test doesn't exercise it.
- **Verified**: `node --test test/fsutil.test.js` — 6 pass, 1 fail (same failure as before the fix: `expected: 2, actual: 3` on the literal-string assertion, unrelated to the `src/fsutil.js` change). `npm test` not run to completion pending resolution of this gap.
- **Decisions/notes**: Per task instructions I did not modify `test/fsutil.test.js`. The gap needs one of: (a) the Task-1 test rewritten to call `forceRemove` (or an exported depth-check helper) with a `path.win32`-style resolution so it actually exercises the fixed code path, or (b) explicit confirmation that this literal-expression assertion was intentionally testing the *formula* in isolation and the task's "all assertions pass" verification criterion needs updating to account for it. Returning to plan-architect / orchestrator for a decision rather than guessing and editing the test file against explicit instructions.
- **Next**: Task 2 remains unchecked in tasks.md pending resolution of the above gap; Task 3 (remove dead BETA_PUBLISHING.md link) is independent and unblocked.

## 2026-07-09T03:05:00Z — Task 2 (resolved by orchestrator): fixed the decoupled test assertion, closed Gap G-1
- **Status**: DONE
- **Changed**: `src/fsutil.js` (exported a new pure `pathDepth(p)` helper — extracted the exact depth-computation logic `forceRemove` already used internally, so it's directly unit-testable without touching the filesystem or duplicating the formula in the test), `test/fsutil.test.js` (rewrote the Gap G-1 assertion to call `pathDepth('C:\\Users\\name')` / `pathDepth('C:/Users/name')` directly instead of computing a hardcoded literal split expression that never invoked source code; added a companion assertion that a deeper Windows path is still allowed through).
- **Root cause of the blocker**: Task 1's Windows-gap test computed `'C:\\Users\\name'.split(/[\\/]/).filter(Boolean).length` as a standalone literal expression — it never called `forceRemove` or anything from `src/fsutil.js`, so no source fix could ever make it reflect the real guard behavior. Resolved by extracting `pathDepth` as an exported, directly-testable unit of the guard's logic (no behavior change to `forceRemove` itself beyond the refactor — it now calls `pathDepth` instead of inlining the computation).
- **Verified**: `node --test test/fsutil.test.js` — 7/7 pass. `npm test` — 61/61 pass, 0 fail.
- **Next**: Task 3 — Remove dead `BETA_PUBLISHING.md` link from README.

## 2026-07-09T02:51:26Z — Task 3: Remove dead `BETA_PUBLISHING.md` link from README
- **Status**: DONE
- **Changed**: `README.md` — removed the `See [BETA_PUBLISHING.md](BETA_PUBLISHING.md) for the full step-by-step guide.` line (and its preceding blank line) between the "Installing a beta" code block and the `## Troubleshooting` heading. No replacement text needed: the Publishing section already contains the full Stable release / Beta release / Installing a beta content inline, so removing the dangling link line leaves the section reading coherently.
- **Verified**: `grep -n "BETA_PUBLISHING" README.md` — zero matches. Manually re-read the `## Publishing` section (lines 467-511) — it now ends naturally after the beta-install code block, no dangling reference.
- **Decisions/notes**: No new file created, per task instructions (content was already fully inline, link was pure duplication).
- **Next**: Task 4 — Final full verification pass.
